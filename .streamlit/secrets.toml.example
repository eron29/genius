# Copie este arquivo para .streamlit/secrets.toml para uso local,
# ou cole o conteúdo em "Settings > Secrets" no Streamlit Community Cloud.
# NUNCA faça commit do arquivo secrets.toml real (ele já está no .gitignore).
#
# PROVEDOR e MODELO definem o que a aba "Seleção de Modelos" usa (sem campos
# manuais na tela). PROVEDOR deve ser "Groq" ou "OpenAI"; MODELO deve ser um
# dos valores listados em CONFIG_MODELOS (core.py) para o provedor escolhido.

PROVEDOR = "Groq"
MODELO = "llama-3.1-70b-versatile"

OPENAI_API_KEY = "sua-chave-openai-aqui"
GROQ_API_KEY = "sua-chave-groq-aqui"
